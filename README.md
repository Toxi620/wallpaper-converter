# 动态壁纸视频转平台

## 目录
- `convert_wallpaper.py` — 主脚本
- `build_mac.sh` — 本地 Mac 打包脚本
- `.github/workflows/build.yml` — GitHub Actions 自动打包

## 用法

```bash
# 直接运行（需已安装 ffmpeg）
python convert_wallpaper.py /path/to/input.mp4

# 或使用 GitHub Actions 下载的打包版本
./壁纸转换工具 /path/to/input.mp4
```
